@model MVC_Web.Models.OrderViewModel
@{
    Layout = null;
}

    @{
        WebGrid objWebGrid = new WebGrid(Model.OrderItems);
    }
    @objWebGrid.GetHtml()
    
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
